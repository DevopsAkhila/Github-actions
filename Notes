Exam / Interview Ready Notes

-------------------------------------

actions/setup-java installs JDK + Maven

Maven cache speeds up builds

mvn test ≠ mvn package

GitHub runners already include Java tooling

Java projects require standard directory layout
